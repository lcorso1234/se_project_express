# Express Backend
WTWR backend built with Express and MongoDB. Provides a REST API for managing users and clothing items (create, list, delete, like/unlike) with centralized error handling and a basic middleware auth stub for user context.
